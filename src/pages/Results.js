import React from "react";

const Results = ({ data }) => {
  return (
    <React.Fragment>
      <div className="flex flex-col md:justify-start md:items-center justify-center items-center">
        {/* {data.map((elem, idx) => (
            <table key={idx} className="flex flex-col md:justify-start md:items-stretch">
                
            </table>
        ))} */}
        Hello World
      </div>
    </React.Fragment>
  );
};

export default Results;
